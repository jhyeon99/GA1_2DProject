# Third-Party Notices

MotionCore 2D Basic does not bundle any third-party source code or assets.

## Runtime dependency

The package depends on one Unity package, which is not redistributed here and is installed
separately through the Unity Package Manager under its own license:

- Unity Input System (com.unity.inputsystem), Unity Technologies, distributed under the Unity
  Package Manager terms.

## Demo content

The demo generator assigns Unity's built-in editor sprite (UI/Skin/Background.psd) to the
generated demo objects for visualization. That built-in resource ships with the Unity Editor and
is not redistributed by this package.

No other third-party materials are included.
